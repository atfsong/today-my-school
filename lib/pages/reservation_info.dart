import 'package:flutter/material.dart';

class ReservationInfoPage extends StatelessWidget {
  const ReservationInfoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('예약 정보 입력 페이지!');
  }
}
