import 'package:flutter/material.dart';

class ReservationStatusPage extends StatelessWidget {
  const ReservationStatusPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('예약 현황 페이지!');
  }
}
